<!DOCTYPE html>
<html>
  <head>
    <title>Athena</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  </head>

  <body>
    <div class="container-fluid">
      <div class="row">
        <% if !current_user.nil? %>
          <nav id="sidebar" class="col-md-3 col-lg-2 d-md-block bg-light sidebar p-3">
            <div class="position-sticky">
              <ul class="nav flex-column">
                <li class="nav-item text-dark">
                  <p class='text-dark'>
                    Меню
                  </p>
                </li>
                <% if can? :manage, :all %>
                  <li class="nav-item">
                    <%= link_to 'Администратор', admin_panel_index_path, class: 'text-dark bg-light btn btn-outline-secondary'%>
                  </li>
                <% else %>
                    <%= link_to 'Пользователь', account_path, class: 'text-dark bg-light btn btn-outline-secondary'%>
                <% end %>
                <li class="nav-item text-dark">
                  <a class="text-dark bg-light btn btn-outline-secondary" href="#">
                    Bootstrap
                  </a>
                </li>
                <li class="nav-item text-dark">
                  <a class="text-dark bg-light btn btn-outline-secondary" href="#">
                    Bootstrap
                  </a>
                </li>
              </ul>
            </div>
          </nav>
        <% end %>

        <main class="col-md-9 ms-sm-auto col-lg-10 px-md-4">
          <div class="my-3">
        <% flash.each do |type, msg| %>
          <% case type %>
          <% when "alert" %>
            <div class="alert alert-danger">
              <%= msg %>
            </div>
          <% when "notice" %>
            <div class="alert alert-success">
              <%= msg %>
            </div>
          <% end %>
        <% end %>
      </div>
      <%= yield %>
        </main>
      </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
  </body>
</html>
